#include "main.h"

/**
* _abs - Entry pointddscxds
* Description: 'the program's description'
* Return: Always 0 (Success)
* @c: int parameter
*/

int _abs(int c)
{


if (c < 0)
{
	c = c * -1;
	return (c);
}
else
{
	return (c);
}
return (0);

}
