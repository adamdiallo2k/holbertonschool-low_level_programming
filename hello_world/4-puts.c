#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
char greetings[] = "Programming is like building a multilingual puzzle";	 
	puts(greetings);
return (0);
}
